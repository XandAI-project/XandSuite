# Default backend host for development
# Change to your desktop machine's local IP address
EXPO_PUBLIC_DEFAULT_HOST=http://192.168.1.100:3847

# Optional API token (must match the desktop app's Mobile API token setting)
# EXPO_PUBLIC_DEFAULT_TOKEN=
